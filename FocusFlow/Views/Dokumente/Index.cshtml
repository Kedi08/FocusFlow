@model IEnumerable<FocusFlow.Models.Dokument>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Typ)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pfad)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Typ)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Pfad)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DokumentId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DokumentId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DokumentId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
